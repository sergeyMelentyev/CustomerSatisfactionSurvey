import React from "react";
import "./dashboard.css";

export default class Landing extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <p>Dashboard</p>
        );
    }
};